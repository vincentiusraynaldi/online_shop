import { Router } from "express";
import { DI } from "../";
import passport from "passport";

const router = Router({mergeParams: true})

// add wishlist

// show all wishlist

// show all items in wishlist

// update wishlist

// add item in wishlist

// delete wishlist

// delete item in wishlist

export const wishlistRouter = router;